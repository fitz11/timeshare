name: timeshare
description: "A Flutter calendar sharing app."

publish_to: "none"

version: 2.0.1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  table_calendar: ^3.1.3
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  flutter_riverpod: ^3.1.0
  flutter_launcher_icons: ^0.14.4
  rxdart: ^0.28.0
  logger: ^2.5.0
  uuid: ^4.5.1
  flutter_secure_storage: ^10.0.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.5.4
  freezed: ^3.0.6
  json_serializable: ^6.9.5
  # NOTE: custom_lint and riverpod_lint temporarily removed due to
  # incompatibility with Dart SDK 3.9.2. Re-add when riverpod_lint
  # releases an update supporting the newer analyzer versions.
  # custom_lint: ^0.8.0
  # riverpod_lint: ^3.1.0
  mockito: ^5.4.4
  clock: ^1.1.1

flutter:
  uses-material-design: true

  assets:
    - assets/timeshareimg.png
